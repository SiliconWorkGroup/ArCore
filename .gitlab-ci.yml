image: ubuntu:latest

stages:
  - build

before_script:
  - apt search libicu
  - chmod +x cpwrapper

build:
  stage: build
  script:
    ./cpwrapper build
  artifacts:
    paths:
      - build/
    expire_in: 1 week